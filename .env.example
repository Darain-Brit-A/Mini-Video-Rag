# Optional: enables LLM-based grounded answer wording.
# Without this key, the app uses a simple extractive fallback.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional local model settings.
WHISPER_MODEL=small
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
